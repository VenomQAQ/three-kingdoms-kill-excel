services:
  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

  # M2+: wire server to Redis for multi-instance room state
  # server:
  #   build: ./server
  #   ports:
  #     - '3000:3000'
  #   environment:
  #     REDIS_URL: redis://redis:6379
  #   depends_on:
  #     - redis

volumes:
  redis_data:
